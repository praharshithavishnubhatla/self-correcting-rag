NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Must match the backend's API_KEY (see ../.env.example). Required to upload
# documents or trigger an eval run; browsing/asking questions works without it.
NEXT_PUBLIC_API_KEY=
